package: com.example.edl
rootException: ExampleEdlException
source: example-service
categories:
  Validation:
    codePrefix: VAL
  NotFound:
    codePrefix: NF
errors:
  userNotFound:
    category: NotFound
    fixed:
      code: 1
      description: "User {userId} not found"
      detail: "User {userId} not found detail"
    required:
      userId: String
